import { RaceGoingPipePipe } from './race-going-pipe.pipe';

describe('RaceGoingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RaceGoingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
